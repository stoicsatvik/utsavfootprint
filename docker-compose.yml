services:
  sensor-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      UTSAV_DB_PATH: /data/utsav.db
    volumes:
      - utsav-data:/data
    restart: unless-stopped
volumes:
  utsav-data:
